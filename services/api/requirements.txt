fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
pydantic>=2.7.0,<3.0.0
httpx>=0.27.0,<1.0.0
tenacity>=8.0.0,<9.0.0

chromadb>=0.5.0,<1.0.0
# Pin posthog: chromadb 0.5/0.6 calls the old capture() signature; posthog 3+ breaks it.
posthog>=2.4.0,<3.0.0
numpy>=1.26.0,<3.0.0
# Phase 2a vector backend (RAG_BACKEND=qdrant). Embedded local mode (on-disk path,
# no server) -- mirrors Chroma's posture. Chroma stays the default until parity is
# proven live; both go through services/api/ragstore.py.
qdrant-client>=1.7.0,<2.0.0

# Embeddings for RAG (lean tier): fastembed uses onnxruntime, no torch.
# This is the default node embedding backend (EMBED_BACKEND=auto|fastembed).
# The sentence-transformers/torch fallback is an OPT-IN extra; install
# services/api/requirements-embed-torch.txt and set EMBED_BACKEND if you want it.
fastembed>=0.3.6,<1.0.0
